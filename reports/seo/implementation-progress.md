# SEO Migration & Deployment — Implementation Progress

Session started 2026-07-15 02:30 BST. Working branch: `main` (HEAD at start: `2afeb576` "seo").

## Initial state (verified this session)

- Working tree clean apart from untracked `claude-seo/` (SEO skill install; left untouched).
- Netlify production deploys fail at Edge Functions bundling: manifest `methods: ["GET","HEAD"]`
  rejected (`HEAD` not in allowed enum). Source: `netlify/edge-functions/reject-noncanonical-paths.ts`
  declared `method: ["GET", "HEAD"]`.
- Live production (silverstone-ai.com) still serves the last successful deploy (pre-`318527e2`):
  - Pretty URLs ON: `/about` → 301 → `/about/` (trailing-slash 200s).
  - Broad SPA fallback active: `/no-such-page-xyz` → 200 (soft 404); `/industries/dentists` → 200.
  - `/niches/dentists` → 301 → `/services/dentists` (which now itself has no page → chain risk).
  - Live sitemap: 46 slashless URLs, `/industry/*` style, 21 OLD (long) blog slugs.
  - Old blog slugs live; current tree (`10dcca0a`) renamed all 21 to short slugs — migration map needed.
- Local tree already contains the previous (undeployed) SEO pass (`318527e2`):
  - Pretty URLs disabled, single www→apex redirect, SPA fallback removed, edge guard 404s
    trailing-slash//`.html`/repeated-slash variants, sitemap = 46 URLs from prerendered HTML.
- Historical evidence located:
  - `git show 318527e2^:netlify.toml` — full legacy redirect set (.html cores, /niches/*→/services/<industry>,
    /services/<industry>.html, trades-virtual-office, /blog/*→/blog, SPA fallback).
  - `web/src/data/generated/legacy-route-manifest.json` — 133-row legacy URL baseline with dispositions.
  - `docs/silverstone-transformation/ROUTE_MANIFEST.md`, `REDIRECT_REQUIREMENTS.md` (2026-07-14 contract).

## Phase 1 — Netlify build blocker: FIXED (locally verified)

- `netlify/edge-functions/reject-noncanonical-paths.ts`: `method: ["GET","HEAD"]` → `method: ["GET"]`.
- Added `web/tests/unit/edge-function-config.test.ts` (fails if any non-enum method reintroduced;
  also pins pattern behavior + www deferral). 4/4 passing.
- Ran exact production command `npm --prefix web ci && npm --prefix web run build:production` → success,
  46-URL sitemap written.
- Ran `npx netlify-cli build --offline` → Edge Functions bundling succeeded; generated manifest at
  `.netlify/edge-functions-dist/manifest.json` contains `"methods": ["GET"]` only (bundler 14.10.3).
- Real Netlify deploy confirmation pending final deploy phase.

## Decisions

- Edge function will be extended (same file) to 301 recognised noncanonical variants to their canonical
  one-hop destination instead of blanket 404, using:
  - a build-generated canonical-path list (`netlify/edge-functions/generated/`, gitignored, generated by
    a dependency-free node script importing the same authoritative sources as `react-router.config.ts`);
  - a committed hand-written legacy migration map (`netlify/edge-functions/lib/url-migration.ts`) shared
    with tests and kept in sync with `netlify.toml` by unit test.
- Clean legacy paths (no trailing slash/.html) are handled by explicit `[[redirects]]` in `netlify.toml`
  (CDN-native, no edge invocation); edge handles only pattern-matched variants.
- Deleted blog placeholders → 410 rules (verify Netlify serves 410 on deploy preview; documented 404
  fallback otherwise).
- Sitemap: split into index + pages + posts with accurate lastmod (post dates from JSON-LD/blog data;
  page dates from a maintained date map). No build-timestamp lastmod.

## Open items

- Historical URL inventory subagent running (blog rename map, placeholder slugs, /industries evidence,
  full netlify.toml redirect history).
- Phases 2–17 pending.

## Phases 2–14 — COMPLETE (2026-07-15 morning)

- URL inventory: 53 one-hop 301s + 45 410s in `netlify/edge-functions/lib/url-migration.mts`
  (evidence-annotated), emitted to `_redirects` at build; edge function 301s recognised
  variants (incl. www one-hop), 410s gone paths, genuine 404 otherwise.
- Sitemap: index + pages(25) + posts(21), content-date lastmod, build-fails-on-drift gates.
- SEO fixes: article OG/twitter/date metadata, absolute og:image, home title, dental dup title,
  Service areaServed UK, 7 industry Service.name reconciliations, Organization logo/sameAs/address,
  publisher logo, 404.html title+noindex, security headers, /invoices noindex, 410.html lang.
- Audits: Agentic-SEO (orchestrator + direct equivalents; live scripts post-deploy) +
  claude-seo (5 specialist subagents). Consolidated in consolidated-seo-findings.csv;
  2 agent claims refuted and suppressed (JSON-LD "missing", FAQ visibility conflict resolved).
- Validation: vitest 159/159; lint clean; typecheck clean; Playwright 82 passed/0 failed
  (staging flow); netlify-cli edge bundling valid (methods=[GET]); TOML parses; staging
  safety branch verified (Disallow robots, no sitemap).
- Regression: byte-level body diff vs 2afeb576 baseline — identical (one invisible
  animation-seed attr); see visual-regression-report.md.

## Next: deploy (push to main), watch Netlify build, live URL matrix, independent verifier.

## Phase 15–16 — DEPLOYED & LIVE-VERIFIED (2026-07-15 16:41Z)

Netlify production deploy (from `get-deploy` API):
- Deploy ID: `6a57b827146f7d000835d2f3` (build `6a57b827146f7d000835d2f1`)
- Commit: `6fab6f7582a395013a00cfa3ceecaad2f314875d` (branch main, context production)
- State: **ready**, error_message: null, deploy_time 39s, published 2026-07-15T16:41:51Z
- edge_functions_present: true — "1 edge function deployed", "99 redirect rules
  processed — All redirect rules deployed without errors", "3 header rules
  processed", secret scan 3310 files / 0 matches.
- This is the same edge-bundling step that previously failed on the HEAD method.

Two intermediate deploys preceded it in this session:
- `ce6f9839` (initial migration) then `80ddbadf` (n8n auto-published the 22nd
  article — live proof the blog automation flows into the sitemap unattended)
  + `e6f298c1` (floored the post-count test), then `6fab6f75` (forced redirects
  + www edge cleanup after the first live matrix surfaced /index and www hops).

Live URL matrix (reports/seo/production-url-matrix.csv): **191 checks, 0 failures.**
- All 47 canonicals: direct 200, self-canonical, index,follow (privacy noindex).
- 53 legacy 301s + variant 301s: one hop to canonical, destination 200.
- 45 deleted articles: genuine 410. Unknown paths: genuine 404 (no 200 shell).
- HEAD requests (200/301/404/410) all correct at the CDN — the original blocker,
  now verified working live.
- www: 301 to apex with query preserved (www+variant = accepted 2 hops).
- /index 301->/; /About served 200 by Netlify case-insensitively but with a
  correct self-canonical to /about (search engines consolidate it).
- sitemap.xml index + 2 children all 200; robots.txt 200 text/plain.
- Newest auto-published article present in sitemap-posts.xml, 200 live, with
  BlogPosting JSON-LD + visible H1 in initial HTML.
