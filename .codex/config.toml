# FILE: .codex/config.toml
# Codex CLI configuration for this repo.
# Web search + network are enabled so Codex can validate browser/event behavior.
# Sandbox is set to write so Codex can implement changes after evidence-backed diagnosis.

model = "gpt-5.2-codex"
sandbox = "write"
web_search_request = true
network_access = true
