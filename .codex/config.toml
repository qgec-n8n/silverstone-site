# FILE: .codex/config.toml
# NOTE: Network + web search are enabled so Codex can read the required OpenAI Cookbook docs
# referenced by title in ExecPlan / codex docs (URLs are provided outside of repo files).

model = "gpt-5.2-codex"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
network_access = true
web_search_request = true
