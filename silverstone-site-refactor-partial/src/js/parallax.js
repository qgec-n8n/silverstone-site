/*
 * parallax.js
 *
 * Controls parallax backgrounds for sections with `.parallax-section`.
 * Handles mobile fallbacks via `.parallax-mobile-stage` and
 * `.parallax-mobile-layer`, and updates background positions on scroll.
 */

export function initParallax() {
  // TODO: Move parallax logic from assets/js/script.js here.
}