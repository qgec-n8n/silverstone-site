Role: Act as a senior Replit project architect, React Router engineer and application-validation specialist.

Task type: Post-integration verification and Replit configuration confirmation.

Do not begin redesigning or substantially modifying the website.

OUTCOME

Confirm that the repaired GitHub `main` branch has been pulled successfully into Replit, that the complete React application and authoritative project documentation are now present, and that Replit consistently runs and previews `/web` rather than the legacy root website.

Make only minimal Replit-specific configuration corrections when they are conclusively necessary.

Do not push changes to GitHub during this task.

PROJECT BOUNDARY

The repository contains two website implementations:

1. The legacy vanilla HTML, CSS and JavaScript website at the repository root.
2. The active React Router/Vite application under `/web`.

`/web` is the only active application.

The root vanilla website is frozen legacy migration evidence.

Do not edit or redesign the legacy root website.

Do not begin a visual redesign, animation implementation, content rewrite or feature build.

SAFETY RULES

* Do not create, switch, merge, rebase or delete Git branches.
* Do not push to GitHub.
* Do not deploy or publish anything.
* Do not change DNS.
* Do not change Netlify configuration.
* Do not enable production analytics.
* Do not enable production email delivery.
* Do not enable production booking integrations.
* Do not add secrets.
* Do not delete the legacy root website.
* Do not recreate `/web`.
* Do not install a replacement framework.
* Do not commit generated dependencies, caches or build output.
* Do not include environment-generated prompt attachments in any commit.
* Preserve uncommitted user work.

PHASE 1 — CONFIRM THE GITHUB REPAIR WAS PULLED

1. Inspect:

   * current working directory;
   * current branch;
   * current commit SHA;
   * `git status`;
   * configured remotes;
   * local and remote branch state.

2. Confirm the current branch is `main`.

3. Fetch remote metadata without changing branches:

   `git fetch origin --prune --tags`

4. If the clone is shallow, fetch sufficient history to verify branch ancestry.

5. Confirm local `main` and `origin/main` point to the same commit.

6. If local `main` is behind `origin/main` and the working tree is safe to update:

   * pull using a fast-forward-only update;
   * do not merge unrelated local changes;
   * do not discard user work.

7. If local `main` and `origin/main` cannot be aligned safely, stop and report the exact blocker.

8. Confirm that the transformation audit history is now included in `main`, using branch ancestry checks where the remote branch is available.

9. Confirm that the following now exist:

   * `/web/package.json`;
   * the `/web` lockfile;
   * `/web/src/`;
   * `/web/public/`;
   * application routes;
   * application components;
   * application styles;
   * application tests and validation scripts;
   * `/web/AGENTS.override.md`;
   * `/docs/silverstone-transformation/`;
   * architecture documentation;
   * audit documentation;
   * content documentation;
   * brand and motion design documentation;
   * visual-direction documentation.

10. Confirm the required source and documentation files are non-empty.

11. If any required source or documentation is still missing, stop with:

`BLOCKED: The repaired GitHub main branch is not present in this Replit project.`

Do not fall back to the legacy website.

PHASE 2 — HANDLE REPLIT-GENERATED LOCAL ARTIFACTS SAFELY

1. Inspect any staged or untracked files under:

   * `attached_assets/`;
   * temporary upload directories;
   * generated cache directories.

2. Do not commit or push pasted prompt attachments.

3. If an environment-generated prompt attachment is staged but is not part of `origin/main`:

   * remove it from the Git staging area without altering repository source;
   * leave it untracked or remove it only when it is conclusively a disposable copy of the current prompt;
   * report what was done.

4. Do not reset, clean or discard unrelated user files.

PHASE 3 — READ THE DURABLE PROJECT CONTEXT

Read:

* root `replit.md`;
* root `.replit`;
* root `AGENTS.md`;
* `/web/AGENTS.override.md`;
* root `package.json`;
* `/web/package.json`;
* relevant lockfiles;
* relevant React Router, Vite, TypeScript and test configuration;
* applicable manifests and authoritative documents under `/docs/silverstone-transformation/`.

Confirm that root `replit.md` clearly states:

* `/web` is active;
* the root website is legacy migration evidence;
* active commands run from `/web`;
* project documentation is authoritative;
* production changes are prohibited.

Do not broadly rewrite `replit.md` if it is already correct.

PHASE 4 — CONFIRM AND CORRECT REPLIT EXECUTION

1. Inspect the root `.replit`.

2. Confirm that the Run button starts the React application from `/web`.

The command must be functionally equivalent to:

`cd web && npm run dev -- --host 0.0.0.0 --port ${PORT:-3000}`

3. Confirm that `.replit` does not:

   * run root `npm start`;
   * serve the repository root;
   * use the root legacy static server;
   * set `publicDir = "."` as the active application;
   * configure the legacy website as the current Replit deployment.

4. If Replit regenerated or retained stale legacy configuration, make the smallest necessary correction to `.replit`.

5. Do not add or change a production deployment configuration.

6. Stop any currently running legacy root server.

7. Use the Replit Run button or the configured run command to start `/web`.

8. Confirm the active server:

   * runs from `/web`;
   * binds to `0.0.0.0`;
   * uses the Replit-provided port;
   * opens successfully in Preview.

PHASE 5 — VALIDATE THE ACTIVE APPLICATION

From `/web`:

1. Confirm the available Node version satisfies the version declared in `/web/package.json`.

2. Use the package manager and lockfile already declared by the project.

3. Perform a clean dependency installation.

4. Run the applicable existing scripts, including:

   * formatting check;
   * lint;
   * strict type checking;
   * unit tests;
   * migration validation;
   * staging build;
   * bundle report;
   * staging-safety validation;
   * browser or Playwright tests where supported.

5. Use App Testing and Preview to inspect representative routes, including where available:

   * home;
   * one service page;
   * one industry page;
   * about;
   * pricing;
   * contact;
   * booking;
   * one resource or article route.

6. Test desktop and mobile viewport behaviour.

7. Confirm:

   * routes load;
   * navigation works;
   * React application styles load;
   * application assets load;
   * no fatal browser-console errors occur;
   * the rendered pages originate from `/web`;
   * the application is not an iframe, proxy or static fallback to the legacy root;
   * staging analytics remain disabled;
   * production email delivery remains disabled;
   * staging indexing remains blocked;
   * no production deployment occurs.

8. Do not make broad application-code repairs.

9. For any unrelated application or test failure:

   * report the exact command;
   * report the exact error;
   * distinguish code failure from Replit-environment limitation;
   * do not conceal it;
   * do not expand the task into an unrelated feature or redesign.

PHASE 6 — FINAL VERIFICATION

Confirm:

* local `main` matches `origin/main`;
* the complete `/web` source is present;
* authoritative documentation is present;
* `/web` is the active Replit application;
* the Run button starts `/web`;
* Preview displays `/web`;
* root `npm start` is not the default Replit workflow;
* the root repository is not being served as the active site;
* stale legacy static deployment metadata does not control Preview;
* root `replit.md` is present and accurate;
* `/web/AGENTS.override.md` is present;
* legacy root source remains intact;
* production and Netlify configuration remain untouched;
* no deployment occurred;
* no secrets were added;
* no redesign or major implementation began;
* no generated dependency or build folders were added to Git;
* no prompt attachment is staged for commit.

FINAL REPORT

Provide:

1. Local `main` SHA.
2. Remote `origin/main` SHA.
3. Whether they match.
4. Whether the transformation history is now included.
5. Required `/web` files verified.
6. Required documentation verified.
7. Root `replit.md` result.
8. Root `.replit` result.
9. Any Replit configuration correction made.
10. Node and package-manager versions.
11. Commands run.
12. Validation results.
13. Preview port and URL path used.
14. Representative routes inspected.
15. Browser-console result.
16. Existing failures or Replit limitations.
17. Confirmation that the legacy root website was not changed.
18. Confirmation that production configuration was not changed.
19. Final `git status`.

End with exactly one of:

`READY: /web is present, validated and running as the active Replit application.`

or

`BLOCKED: Do not begin further implementation work.`

After reporting, stop. Do not infer or begin the next task.
