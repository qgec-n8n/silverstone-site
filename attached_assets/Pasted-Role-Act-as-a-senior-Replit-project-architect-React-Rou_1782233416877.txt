Role: Act as a senior Replit project architect, React Router engineer, repository analyst, visual-engineering technical lead and quality-assurance engineer.

Task type: Repository preparation, configuration, discovery and validation only.

OUTCOME

Develop a complete and accurate understanding of the Silverstone AI repository, correct Replit’s mistaken focus on the legacy root website, establish `/web` as the active application, validate the application baseline and prepare the project for subsequent implementation work.

Do not begin redesigning, substantially modifying or expanding the website during this task.

The purpose of this task is to ensure that Replit:

* understands the complete repository;
* understands which application is active;
* understands the project’s business and conversion objectives;
* understands the authoritative architecture, content, design and quality requirements;
* runs and previews the correct application;
* preserves the legacy application as migration evidence;
* is ready to respond accurately to a separate implementation prompt provided later.

The repository is intended to be developed from its current merged `main` state.

Do not create, switch to, merge, rebase, cherry-pick or rely on any transformation branch.

PROJECT CONTEXT

Silverstone AI is rebuilding its website into a premium, high-conversion React application for non-technical UK business decision-makers.

The primary conversion is a qualified discovery-call booking.

The required service families are:

* Website Design & Development
* App Development
* AI Voice Agents
* AI Receptionists
* Content Creation
* AI Agents & Automation Workflows

The repository intentionally contains two website implementations:

1. A legacy vanilla HTML, CSS and JavaScript website at the repository root.
2. A new React Router/Vite application under `/web`.

The `/web` application is the only active development target.

The root vanilla website is frozen legacy migration evidence. Its presence does not mean it is the active application.

The legacy root website must not be edited, redesigned, rebuilt, served by default or treated as the implementation target unless a future user instruction explicitly identifies a legacy root file.

All future implementation work for the rebuilt Silverstone AI website should occur under `/web` unless the user explicitly instructs otherwise.

AUTHORITATIVE PROJECT MATERIAL

The project contains authoritative research, audits, architecture, content, design, motion, migration, testing and handoff materials under:

`/docs/silverstone-transformation/`

These documents must be understood and used as evidence for future implementation decisions.

The documentation-directory name is historical and does not instruct Replit to create, use or search for Git transformation branches.

NON-NEGOTIABLE SAFETY RULES

* Do not deploy or publish anything.
* Do not change DNS.
* Do not change Netlify publishing or production deployment settings.
* Do not enable production analytics.
* Do not enable production Resend email delivery.
* Do not connect production Calendly or other live production integrations.
* Do not add real secrets or production environment values.
* Do not push to GitHub.
* Do not create or change Git branches.
* Do not rewrite or remove authoritative documentation.
* Do not delete the legacy website.
* Do not begin a visual redesign or large implementation task.
* Do not make unrelated application changes.
* Do not overwrite uncommitted user work.
* Do not install a replacement framework.
* Do not recreate the application from scratch.
* Do not move the `/web` application into the repository root.
* Do not make the root `package.json` the active application package.
* Do not treat generated output, dependency directories or cache files as source code.
* Do not fabricate client results, testimonials, guarantees, performance statistics or integrations.
* Do not alter approved content merely to make configuration or validation easier.

Use Replit checkpoints before changing configuration where available.

EXECUTION PROTOCOL

Perform this work in six mandatory sequential phases:

Phase 1 — Safety and application-presence gate
Phase 2 — Repository and documentation discovery
Phase 3 — Root-cause diagnosis
Phase 4 — Replit configuration and durable project context
Phase 5 — Application baseline validation
Phase 6 — Future-work readiness assessment and final report

Complete each phase before proceeding to the next.

After each phase:

* inspect the current working-tree state;
* review every change made;
* confirm no unrelated files were altered;
* confirm the legacy root website remains untouched;
* confirm production controls remain untouched;
* record a concise internal checkpoint.

If the `/web` application is missing or materially incomplete, stop rather than attempting to create a replacement.

PHASE 1 — SAFETY AND APPLICATION-PRESENCE GATE

Before modifying anything:

1. Inspect:

   * the current working directory;
   * current Git branch;
   * current commit;
   * `git status`;
   * tracked and untracked changes;
   * available Replit checkpoints;
   * repository root contents;
   * `/web` contents.

2. Confirm that the local project contains, at minimum:

   * `/web/package.json`;
   * a `/web` lockfile;
   * `/web/src/`;
   * React Router or Vite configuration;
   * application routes;
   * application styles;
   * application tests or validation scripts;
   * `/docs/silverstone-transformation/`.

3. Inspect `/web/package.json` and confirm that it represents the intended React application.

4. Inspect the Git history sufficiently to determine whether the imported local `main` includes:

   * the rebuilt React application;
   * project documentation;
   * content implementation;
   * design-system implementation;
   * route and migration work;
   * test and validation infrastructure.

5. If `/web/package.json`, `/web/src/` or the authoritative project documentation is absent, stop immediately.

Do not fall back to the legacy root website.

In that situation, end with:

`BLOCKED: The active /web application is missing or incomplete in the imported repository.`

Identify exactly what is missing and provide evidence from the local repository.

6. If uncommitted user work exists:

   * preserve it;
   * do not discard, reset, overwrite or clean it;
   * determine whether preparation can be completed safely around it;
   * stop and report the conflict if safe preparation is not possible.

PHASE 2 — REPOSITORY AND DOCUMENTATION DISCOVERY

Build an accurate internal map of the repository before editing configuration.

Read all applicable project-instruction and configuration files, including:

* `/replit.md`, if present;
* `/.replit`;
* `/AGENTS.md`;
* `/web/AGENTS.override.md`, if present;
* root `package.json`;
* `/web/package.json`;
* relevant lockfiles;
* React Router configuration;
* Vite configuration;
* TypeScript configuration;
* ESLint configuration;
* Playwright configuration;
* Vitest configuration;
* environment examples;
* staging environment files;
* application entry points;
* route definitions;
* content and route-data sources;
* global styles;
* design tokens;
* relevant scripts;
* Replit workflows;
* port configuration.

Read the relevant authoritative documentation under `/docs/silverstone-transformation/`.

Prioritise:

* all relevant `MANIFEST.md` files;
* the active execution plan;
* target architecture;
* rebuild decision;
* route-migration implementation;
* staging-environment contract;
* performance budgets;
* quality gates;
* file-ownership register;
* content and information-architecture package;
* content implementation report;
* claims and proof controls;
* migration exceptions;
* brand and motion design system;
* design-system implementation report;
* visual-direction prototype specification;
* responsive and reduced-motion requirements;
* motion storyboards;
* page visual concepts;
* prototype acceptance criteria;
* shader and component decisions;
* sitewide art direction;
* tools-carousel and demo-system specification;
* relevant benchmark research;
* relevant handoff records.

Do not assume every historical document remains operationally current.

Classify instructions as:

* current and authoritative;
* historical evidence;
* superseded;
* implementation guidance;
* acceptance criteria;
* unresolved decision;
* potential conflict.

Create an internal repository map identifying:

* active application location;
* legacy application location;
* framework and versions;
* package manager;
* development command;
* build command;
* preview command;
* testing commands;
* route architecture;
* component architecture;
* styling architecture;
* design-token architecture;
* content architecture;
* asset locations;
* animation libraries;
* testing infrastructure;
* staging safeguards;
* performance budgets;
* accessibility requirements;
* production boundaries;
* authoritative design documents;
* authoritative content documents;
* unresolved dependencies or missing inputs.

Do not begin implementation during this discovery phase.

PHASE 3 — ROOT-CAUSE DIAGNOSIS

Determine exactly why Replit selected and built the legacy vanilla website.

Inspect and compare:

* root `.replit`;
* root `replit.md`;
* root `package.json`;
* `/web/package.json`;
* configured Run button;
* workflows;
* command working directories;
* configured ports;
* deployment metadata;
* any Replit-generated project description;
* framework detection;
* Preview configuration;
* existing running processes.

Explicitly determine whether Replit was directed to the legacy site by any of the following:

* root `npm start`;
* a root static-server command;
* `publicDir = "."`;
* a root static-deployment declaration;
* a workflow executing from the repository root;
* legacy port configuration;
* missing or stale `replit.md`;
* missing `/web` instructions;
* framework auto-detection;
* an earlier Agent-generated configuration;
* a stale running process;
* conflicting root and `/web` package files.

Do not merely state that `/web` should be used.

Identify the exact configuration entries, commands and execution path that caused Replit to select the legacy site.

PHASE 4 — REPLIT CONFIGURATION AND DURABLE PROJECT CONTEXT

Correct Replit so that `/web` is consistently treated as the active application.

A. ROOT `replit.md`

Create or carefully revise `/replit.md`.

It must be located at the repository root.

Preserve any accurate existing guidance, but ensure the resulting file clearly contains the following.

1. Project purpose

   * Silverstone AI is rebuilding its website as a premium React application.
   * The primary conversion is a qualified discovery-call booking.
   * The target users are non-technical UK business decision-makers.
   * The site must remain credible, accessible, performant, responsive and conversion-focused.

2. Active application

   * `/web` is the only active application.
   * All new website development occurs under `/web`.
   * Routes, components, styles, content implementation, tests, assets, scripts and integrations for the rebuilt site belong under `/web`.
   * The root vanilla site is frozen legacy migration evidence.
   * A root `package.json` does not make the root website active.

3. Legacy boundary

   Replit must not edit, rebuild or serve by default:

   * root HTML pages;
   * legacy root CSS;
   * legacy root JavaScript;
   * legacy root assets;
   * root legacy build scripts;
   * `netlify.toml`;
   * `netlify/functions/**`;

   unless a future user instruction explicitly identifies those paths.

4. Required working directory

   Replit must execute active application commands from `/web`.

5. Canonical commands

   Derive the exact commands from `/web/package.json` and record them accurately.

   These may include equivalents of:

   * dependency installation;
   * development server;
   * formatting check;
   * lint;
   * type checking;
   * unit tests;
   * browser tests;
   * accessibility tests;
   * build;
   * migration validation;
   * bundle reporting;
   * staging-safety validation.

   Do not invent scripts that are not present.

6. Documentation authority

   * `/docs/silverstone-transformation/` contains authoritative project specifications, decisions, acceptance criteria and evidence.
   * Replit must inspect the relevant documents before implementing future user instructions.
   * Historical branch references do not instruct current branch operations.
   * Future work occurs in the current local repository state unless the user explicitly states otherwise.

7. Design and content boundaries

   * Approved information architecture, copy, claims controls, design tokens, motion rules, responsive behaviour, reduced-motion behaviour, performance budgets and acceptance criteria must be taken from authoritative project materials.
   * Replit must not substitute generic AI-agency copy or an unrelated template.
   * Replit must not overwrite approved content merely to fit a visual idea.
   * Replit must not fabricate client results, testimonials, guarantees, performance statistics or integrations.

8. Safety boundaries

   * Replit is a non-production staging and editing environment.
   * No production deployment or production-integration mutation is permitted.
   * Analytics, email delivery, booking and indexation must remain staging-safe.
   * Secrets must not be committed.
   * Generated directories and dependency directories must not be committed.

9. Quality expectations

   * Inspect before editing.
   * Use the existing architecture.
   * Preserve unrelated behaviour.
   * Avoid broad refactors.
   * Test all changed behaviour.
   * Use Preview and App Testing for visual changes.
   * Check desktop, tablet and mobile.
   * Respect accessibility and reduced-motion requirements.
   * Record exact files changed and validation results.

10. Future implementation preparation

State that future implementation instructions may require substantial visual, motion, content, interaction or conversion work under `/web`.

Before executing any such instruction, Replit must:

* read the relevant authoritative documents;
* inspect the current implementation;
* identify reusable components;
* preserve approved content and claims controls;
* define validation criteria;
* use Preview and browser testing;
* respect performance and accessibility budgets;
* avoid modifying the legacy root website.

B. ROOT `.replit`

Correct `/.replit` so that the Run button starts the `/web` development application.

The active run path must be functionally equivalent to:

`cd web && npm run dev -- --host 0.0.0.0 --port ${PORT:-3000}`

Use syntax supported by the current Replit environment.

Requirements:

* run from `/web`;
* bind to `0.0.0.0`;
* use the Replit-provided port where available;
* configure Preview to use the active application’s port;
* do not run root `npm start`;
* do not serve the repository root;
* do not use a legacy root workflow;
* do not build the root static site;
* do not configure or trigger a deployment.

Inspect any existing `[deployment]` or static `publicDir` configuration.

If it exclusively identifies the legacy root site and could cause future misidentification:

* remove or neutralise the stale legacy-only Replit deployment metadata;
* do not replace it with an unverified production deployment configuration;
* document the decision.

Do not change Netlify configuration.

C. INSTRUCTION CONSISTENCY

Inspect `/AGENTS.md` and `/web/AGENTS.override.md`.

Ensure they do not actively direct future work toward the root legacy site.

Preserve useful historical and legacy guardrails.

Where required, make the smallest documentation-only clarification so:

* root legacy instructions clearly apply to the legacy implementation;
* active `/web` work follows the `/web` application instructions;
* future agents understand that `/web` is active;
* historical branch references are not treated as current execution instructions.

Do not broadly rewrite these files if they are already correct.

D. REPLIT PROJECT STATE

Stop any stale legacy root server.

Start only the `/web` application.

Ensure the Preview pane displays the React application rather than the vanilla site.

Do not change application visuals during this phase.

PHASE 5 — APPLICATION BASELINE VALIDATION

Validate the active application from `/web`.

1. Confirm that the installed Node version satisfies `/web/package.json`.

2. Use the package manager and lockfile already declared by the project.

3. Perform a clean dependency installation from `/web`.

4. Start the `/web` development server.

5. Confirm in Preview that the rendered application comes from `/web`.

6. Confirm the application does not merely display the legacy root website through:

   * a proxy;
   * an iframe;
   * copied root static pages;
   * a fallback static-server route.

7. Inspect several representative routes where available, including:

   * home;
   * a service page;
   * an industry page;
   * about;
   * pricing;
   * contact;
   * booking;
   * a representative article or resource route.

8. Confirm:

   * routes load;
   * navigation works;
   * application styles load;
   * local assets load;
   * no immediate fatal console error exists;
   * the application uses the intended React architecture;
   * mobile and desktop Preview both render;
   * staging safeguards remain active.

9. Run the existing baseline checks from `/web`.

At minimum, run applicable versions of:

* formatting check;
* lint;
* type checking;
* unit tests;
* application build;
* migration validation;
* staging-safety validation.

Run browser, accessibility and end-to-end tests if they are configured and supported in the environment.

Use existing scripts rather than inventing replacements.

10. Do not make broad application-code repairs during this preparation task.

You may make only the smallest startup or configuration correction necessary to make the existing `/web` application run correctly in Replit.

For any unrelated failing test or pre-existing application issue:

* record the exact failure;
* distinguish an environmental failure from a code failure;
* do not conceal it;
* do not claim it passed;
* do not expand this task into an unrelated repair project.

11. Confirm the legacy root application files were not changed.

PHASE 6 — FUTURE-WORK READINESS ASSESSMENT

Without implementing a redesign or major feature, develop a clear understanding of the implementation foundation available for later user instructions.

Review the design, content, architecture, migration and validation materials and identify:

* the current route inventory;
* existing page templates;
* existing reusable components;
* existing design tokens;
* existing typography and layout systems;
* current motion and animation infrastructure;
* permitted animation libraries;
* current responsive architecture;
* current reduced-motion support;
* current accessibility controls;
* performance budgets;
* content-preservation requirements;
* claims and proof restrictions;
* available application assets;
* missing assets;
* unresolved design decisions;
* likely high-risk implementation areas;
* existing unit and browser-test coverage;
* expected browser and device coverage;
* staging-safety constraints;
* dependencies that future work should reuse;
* technical debt that should not be expanded unintentionally.

Inventory Replit capabilities available for later implementation work, including where available:

* Agent;
* Preview;
* App Testing;
* Code Optimizations;
* Design Canvas;
* Visual Editor;
* browser inspection;
* responsive Preview;
* screenshot comparison;
* image generation;
* web search;
* Shell;
* package management;
* checkpoints;
* file history.

Determine how these capabilities should be used effectively for future implementation tasks.

Do not:

* generate images;
* redesign pages;
* edit visual components;
* implement animations;
* rewrite content;
* add new features;
* alter conversion surfaces;

during this preparation task.

FINAL VERIFICATION

Before reporting completion, verify:

* `/web` exists and is complete enough for development;
* `/web` is the active application;
* the Run button starts `/web`;
* Preview displays `/web`;
* root `npm start` is not the default workflow;
* the root repository is not being served as the active site;
* stale legacy static-deployment metadata no longer controls project detection;
* root `replit.md` exists and accurately describes the repository;
* active instructions consistently identify `/web`;
* authoritative documentation is intact;
* the legacy root website is intact;
* no production configuration was changed;
* no secrets were added;
* no deployment occurred;
* no visual redesign or major implementation was started;
* all preparation changes are minimal and intentional.

FINAL REPORT

Provide a concise but complete report containing:

1. Current branch and commit.
2. Whether the `/web` application was present and complete.
3. The exact reason Replit previously selected the legacy site.
4. Repository architecture summary.
5. Authoritative documents reviewed.
6. Exact configuration files changed.
7. Exact changes made to `replit.md`.
8. Exact changes made to `.replit`.
9. Any instruction files clarified.
10. Commands run.
11. Validation results.
12. Preview route and port used.
13. Representative routes inspected.
14. Existing failures or environment limitations.
15. Risks relevant to future implementation work.
16. Replit capabilities recommended for future implementation work.
17. Confirmation that legacy and production files were not altered.
18. Current working-tree status.

End with exactly one of:

`READY: /web is the active validated Replit application and the repository is prepared for the next user instruction.`

or

`BLOCKED: Do not begin further implementation work.`

After producing the report, stop.

Do not independently begin another task or infer what the next user instruction will contain.
